[metadata]
name = getter
version = 0.1

[options]
packages = getter
python_requires = >=3.7
install_requires =
    numpy

[options.extras_require]
test =
    pytest
    pytest-cov
    black
    isort
    mypy
    pandas
